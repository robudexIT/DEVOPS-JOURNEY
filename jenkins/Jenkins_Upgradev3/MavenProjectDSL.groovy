job('First-Maven-Project-Via-DSL') {
    description("First Maven job generated by the DSL on ${new Date()}, the project is a small Maven project hosted on github- edit by rogmer bulaclac")
    scm {
        git("https://github.com/robudexIT/DEVOPS-JOURNEY.git", 'main')
        { node -> // is hudson.plugins.git.GitSCM
            node / gitConfigName('Rogmer')
            node / gitConfigEmail('rogmer2022.bulaclac@gmail.com')
        }
    }
    triggers {
        scm('* * * * *')
    }
    steps {
        maven('clean package', 'jenkins/Jenkins_Upgradev3/maven-samples/single-module/pom.xml')
    }
    publishers {
        //archive the war file generated
        archiveArtifacts '**/*.jar'
    }
}
